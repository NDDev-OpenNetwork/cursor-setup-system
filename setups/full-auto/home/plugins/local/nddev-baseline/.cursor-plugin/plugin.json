{
  "name": "nddev-baseline",
  "version": "1.0.0",
  "description": "The NDDev working floor as a native Cursor plugin. A plugin rather than a loose rule file: it carries its rule through the manifest, so what this provider owns is one directory it created rather than a directory the product also writes to.",
  "author": {
    "name": "NDDev"
  },
  "homepage": "https://github.com/NDDev-OpenNetwork/cursor-setup-system",
  "repository": "https://github.com/NDDev-OpenNetwork/cursor-setup-system",
  "rules": "./rules"
}
