# NDDev Lifecycle

Use `skills/nddev-builder/SKILL.md`, then read
`skills/nddev-builder/references/installation-lifecycle.md`. Check target-owned
the software lifecycle, `install` and `select`, `launch`, `restore` and
`remove`, without touching a live configuration home.
