[package]
name = "cursor-setup-system"
version.workspace = true
description = "Installs, reselects, restores and removes a complete Cursor CLI harness configuration in a caller-named target directory."
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[[bin]]
name = "cursor-setup-system"
path = "src/main.rs"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
provider-v3 = { workspace = true }
harness-runtime = { workspace = true }

[lints]
workspace = true
